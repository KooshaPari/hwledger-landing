# Research

`hwledger-landing` is a clean, unbadged, LLM-relevant candidate. Its README
describes the site as the production landing page for an LLM capacity planner,
fleet ledger, and desktop inference runtime.

The repo has no local AGENTS.md override, so the parent Phenotype contract applies.
The local package scripts expose `bun run build` as the production validation gate.
